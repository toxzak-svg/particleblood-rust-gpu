# OS-generated files
.DS_Store
Thumbs.db
Desktop.ini

# Editor / IDE settings
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local environment files
.env
.env.*
!.env.example
!.env.sample

# Common dependency/build outputs (safe to ignore if introduced later)
node_modules/
dist/
build/
coverage/

# Local tooling cache
.toolcache/

# Local checkout of the Rust UI engine (tracked in its own Git repo)
# Remove this if you later vendor it here or convert it to a submodule.
rust-ui-micro-engine/
